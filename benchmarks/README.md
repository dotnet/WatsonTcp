This directory stores output from `RunBenchmarks.bat`.

Tracked files in this directory define its purpose and keep the directory present in the repository.

Generated benchmark result files such as `Benchmark-*.txt` are intentionally ignored.
