namespace WatsonTcp.Message
{
    public enum MessageStatus
    {
        Normal,
        Success,
        Failure,
        AuthRequired,
        AuthRequested,
        AuthSuccess,
        AuthFailure
    }
}
